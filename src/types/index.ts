export type optionType = {
    city: string
    country: string
} 

export type breweryType = {
    id: string
    name: string
    address_1: string
    city: string
    state: string
    postal_code: string
    country: string
    phone: string
}